services:
  server:
    image: financial_metrics
    build:
      context: .
    volumes:
      - ~/projects/investing_notes:/code
    ports:
      - 5001:80
    command: uvicorn server:app --host 0.0.0.0 --port 80 --reload
    restart: unless-stopped
    env_file: ~/projects/investing_notes/env